{
  "name": "tom-external-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pokemon TCG Tournament Operations Manager (TOM) External Platform and Sync Suite",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/tom-core && npm run build --workspaces --if-present",
    "start": "node packages/server/dist/index.js",
    "dev": "npm run dev -w packages/web",
    "tunnel": "node tunnel.mjs",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "untun": "^0.2.2",
    "vitest": "^2.0.5"
  }
}
