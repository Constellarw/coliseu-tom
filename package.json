{
  "name": "tom-external-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Pokemon TCG Tournament Operations Manager (TOM) External Platform and Sync Suite",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
