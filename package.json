{
  "name": "tom-external-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pokemon TCG Tournament Operations Manager (TOM) External Platform and Sync Suite",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @tom/core && npm run build -w @tom/server && npm run build -w @tom/web && node scripts/postbuild.mjs",
    "vercel-build": "npm run build",
    "start": "node packages/server/dist/index.js",
    "dev": "npm run dev -w packages/web",
    "tunnel": "node tunnel.mjs",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "untun": "^0.2.2",
    "vite": "^5.4.21",
    "vitest": "^2.0.5"
  }
}
